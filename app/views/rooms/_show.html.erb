<table class="table">
  <tr>
    <th>Sala</th>
    <th>Capacidade</th>
    <th>Prédio</th>
  </tr>

  <tr>
    <td><%= get_room_name(room.id) %></td>
    <td><%= get_room_capacity(room.id) %></td>
    <td><%= get_room_building(room.id) %></td>
  </tr>
</table>
